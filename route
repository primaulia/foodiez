OUR ROUTE PLANS

20 Oct
app.use('/')
  GET /
  GET /profile/:slug
  // after 20 oct class
  GET /search
  POST /search

app.use('login')
  GET /
  POST /

app.use('/register')
  GET /
  POST /

app.use('/reviews')
GET /
  POST /

app.use('/restaurants')
  GET /new
  GET /:slug
  GET /:id
  POST /
  POST /:id
  DELETE /:id

// 20 Oct: New Routes
app.use('/admins)
  GET /register
  POST /register
